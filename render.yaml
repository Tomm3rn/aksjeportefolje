services:
  - type: web
    name: aksjeportefolje
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: aksjeportefolje-db
          property: connectionString

databases:
  - name: aksjeportefolje-db
    plan: free
